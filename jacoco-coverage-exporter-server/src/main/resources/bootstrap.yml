management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus
          - health
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  health:
    probes:
      enabled: true